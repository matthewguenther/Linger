name: image

# The server as a container image, on ghcr.io (T-703). `deploy/compose.yaml`
# pulls `ghcr.io/matthewguenther/linger:latest`, so this is what a host
# actually runs — the desktop installers in release.yml are the other half of
# a release, and this is this half.
#
# Same shape as release.yml, for the same reason: **only a tag publishes.**
#
#   tag v0.2.0            build both architectures and push 0.2.0, 0.2, latest
#   Actions tab (manual)  build both architectures, push nothing — proves the
#                         image still builds without shipping anything
#   pull request          build amd64 only, push nothing, and only when a file
#                         the image is made of changed
#
# **One human step, once ever:** the first push creates the package as
# *private*, and a private package means `docker compose up` fails with
# "unauthorized" for everybody who is not Matt. Open
# github.com/users/matthewguenther/packages/container/linger/settings and set
# it public. There is no flag here that can do it.

on:
  push:
    tags: ["v*"]
  pull_request:
    # Not on every pull request: the image is a full release build of the
    # server and it is slow. These are the files it is actually made of.
    paths:
      - deploy/**
      - crates/**
      - Cargo.toml
      - Cargo.lock
      - .github/workflows/image.yml
  workflow_dispatch:

# The image name is written out rather than derived from `github.repository`,
# which is `matthewguenther/Linger` — a capital letter is not a legal image
# name. It has to match deploy/compose.yaml exactly.
env:
  IMAGE: ghcr.io/matthewguenther/linger

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: arm64 emulation
        # arm64 is built under emulation, which is slow — a Rust release build
        # this way takes the better part of an hour. That is tolerable because
        # only a tag does it. It is here because a friend-group server on a
        # Raspberry Pi or an Ampere box is a normal thing to want, and without
        # this those hosts get "no matching manifest" from `docker compose up`.
        # If emulation ever becomes the problem, the fix is a second job on a
        # native `ubuntu-24.04-arm` runner pushing by digest.
        if: github.event_name != 'pull_request'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - uses: docker/setup-buildx-action@v3

      - name: sign in to ghcr
        if: github.ref_type == 'tag'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: work out the tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          # v0.2.0 publishes 0.2.0 and 0.2, and moves `latest`. `latest` is
          # what compose.yaml asks for, so moving it is what makes
          # `docker compose pull` find a new server.
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
          flavor: latest=${{ github.ref_type == 'tag' }}

      - name: build${{ github.ref_type == 'tag' && ' and push' || '' }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deploy/Dockerfile
          platforms: ${{ github.event_name == 'pull_request' && 'linux/amd64' || 'linux/amd64,linux/arm64' }}
          push: ${{ github.ref_type == 'tag' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # A cache that survives between runs, so a tag build is not a cold
          # `cargo build --release` from scratch every time.
          cache-from: type=gha
          cache-to: type=gha,mode=max
