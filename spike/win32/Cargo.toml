[package]
name = "spike-win32"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_UI_WindowsAndMessaging",
    "Win32_System_Threading",
] }

# Detached from the root workspace: the windows crate only builds on Windows.
[workspace]
