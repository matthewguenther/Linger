{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permission surface for the main window (ARCHITECTURE §7).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "description": "Open a link from a message body in the system browser. Web and mail addresses only — a message must never be able to hand the OS a file path or a custom scheme.",
      "allow": [{ "url": "https://*" }, { "url": "http://*" }, { "url": "mailto:*" }]
    }
  ]
}
