{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permission surface for the main window (ARCHITECTURE §7). The updater plugin is registered in lib.rs but is deliberately absent from this list: the page never checks for or installs an update itself, it calls the app's own update_check / update_install commands in src/updates.rs. Granting updater:default here would hand a WebView the ability to download and run an installer.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "http://*" }, { "url": "https://*" }]
    },
    "notification:default"
  ]
}
